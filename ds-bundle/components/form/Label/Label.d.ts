import * as React from 'react';

/**
 * Label — from @nicarunner/ui@0.1.0.
 */
export interface LabelProps {
  className?: string;
  id?: string;
  style?: react.CSSProperties;
  children?: React.ReactNode;
}

export declare const Label: React.ComponentType<LabelProps>;
