name: OpenWiki Update

# Genera y mantiene la documentación para agentes de código (openwiki/) al
# día, abriendo un PR diario con los cambios. Ver https://github.com/langchain-ai/openwiki
#
# Configuración necesaria:
#   Settings → Secrets and variables → Actions → Secrets → "New repository secret"
#   Name:  OPENROUTER_API_KEY
#   Value: <tu API key de OpenRouter, ver https://openrouter.ai/keys>
#
# Sin ese secret configurado, este workflow falla al intentar correr openwiki.
# La primera vez que corre (sin openwiki/ existente) crea la documentación
# inicial; las siguientes veces solo la actualiza.

on:
  workflow_dispatch:
  schedule:
    # GitHub programa en UTC; 08:00 UTC = medianoche PST.
    - cron: "0 8 * * *"

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install OpenWiki
        run: npm install --global openwiki

      - name: Run OpenWiki
        run: openwiki --update --print
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          OPENWIKI_MODEL_ID: z-ai/glm-5.2

      - name: Create OpenWiki update pull request
        uses: peter-evans/create-pull-request@v7
        with:
          add-paths: openwiki
          branch: openwiki/update
          commit-message: "docs: update OpenWiki"
          title: "docs: update OpenWiki"
          body: |
            Automated OpenWiki documentation update.

            This PR was generated by the scheduled OpenWiki workflow.
